fastapi==0.110.0
uvicorn[standard]==0.29.0
requests==2.31.0
python-dotenv==1.0.1
httpx
supabase # Official Supabase Python client
# If your code specifically imports from 'supabase_py' instead of 'supabase',
# uncomment the line below and remove the line above:
# supabase-py

pydantic>=2.0 # Ensure Pydantic is updated for FastAPI 0.110.0 compatibility
email_validator # For email validation in Pydantic models
fastapi-utils==0.8.0 # For utilities like BackgroundTasks from fastapi_utils
